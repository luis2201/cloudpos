<div class="form-grid">
    <label class="field"><span>Código interno <b>*</b></span><input name="code" value="{{ old('code', $record?->code) }}" maxlength="30" required @readonly($record?->is_system)></label>
    <label class="field"><span>Código SRI <b>*</b></span><select name="sri_code" required @disabled($record?->is_system)>@foreach ($sriPaymentCodes as $code => $label)<option value="{{ $code }}" @selected((string) old('sri_code', $record?->sri_code ?? '01') === (string) $code)>{{ str_pad((string) $code, 2, '0', STR_PAD_LEFT) }} · {{ $label }}</option>@endforeach</select>@if ($record?->is_system)<input type="hidden" name="sri_code" value="{{ $record->sri_code }}">@endif</label>
    <label class="field field--wide"><span>Nombre <b>*</b></span><input name="name" value="{{ old('name', $record?->name) }}" maxlength="150" required @readonly($record?->is_system)></label>
</div>
@if ($record?->is_system)<div class="info-box"><x-icon name="shield" /><span>El código y nombre oficial están protegidos. Puedes configurar su uso operativo.</span></div>@endif
<div class="check-stack check-stack--inline"><label class="check-field"><input name="requires_reference" type="checkbox" value="1" @checked(old('requires_reference', $record?->requires_reference))><span>Exige número de referencia</span></label><label class="check-field"><input name="affects_cash" type="checkbox" value="1" @checked(old('affects_cash', $record?->affects_cash))><span>Afecta efectivo en caja</span></label><label class="check-field"><input name="is_active" type="checkbox" value="1" @checked(old('is_active', $record?->is_active ?? true))><span>Forma de pago activa</span></label></div>
